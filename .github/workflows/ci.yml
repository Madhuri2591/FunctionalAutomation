name: Functional Automation CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v3

      - name: 🧰 Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '22'

      - name: 📦 Install dependencies
        run: npm install

      - name: 🔓 Fix cucumber-js permissions
        run: chmod +x node_modules/.bin/cucumber-js

      - name: 🧪 Run tests
        run: npx cucumber-js --config cucumber.js --tags "@ui"

      - name: 📤 Upload Allure results
        uses: actions/upload-artifact@v4.3.1
        with:
          name: allure-results
          path: allure-results/

    #   - name: 📧 Send email notification
    #     if: success()
    #     uses: dawidd6/action-send-mail@v4
    #     with:
    #       server_address: smtp.gmail.com
    #       server_port: 465
    #       secure: true
    #       username: ${{ secrets.EMAIL_USERNAME }}
    #       password: ${{ secrets.EMAIL_PASSWORD }}
    #       subject: "✅ Workflow Passed: ${{ github.workflow }}"
    #       to: madhuri.dumane@example.com
    #       from: "CI Bot <ci@example.com>"
    #       body: |
    #         Hello Madhuri,

    #         The GitHub Actions workflow '${{ github.workflow }}' for '${{ github.repository }}' has completed successfully.

    #         You can view the results and artifacts in the Actions tab.

    #         Regards,
    #         CI Bot
